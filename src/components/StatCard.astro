---
const { text, statText } = Astro.props;
---

<div class="relative flex flex-col gap-6 md:gap-10 px-6 md:px-8 py-12 md:py-16 w-full md:max-w-[420px] bg-neutral-700/10 rounded-lg bg-stat-card stroke-argument-card">
  <slot />
  <p class="text-[50px] md:text-[80px] text-white font-display font-normal">{statText}</p>
  <p class="text-lg md:text-xl text-white font-semibold">{text}</p>
</div>