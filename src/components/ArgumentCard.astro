---
const { text } = Astro.props;
---

<div class="relative flex flex-col gap-6 md:gap-10 px-6 md:px-8 py-12 md:py-16 w-full md:w-[320px] bg-neutral-700/10 rounded-lg bg-argument-card stroke-argument-card">
  <slot />
  <p class="text-lg md:text-xl text-white font-semibold">{text}</p>
</div>